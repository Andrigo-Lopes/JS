let nave = {  // pode ser vazio ou usar o new object()
    name: "Fenix",
    tripulantes: 8,
    tipoDeNave: "Batalha"
}
console.log(nave)
//ou console.log([name])

let brasileirao = {
    times: "inter",
    serie: "A",
    tipoDeCampeonato: "Futebol"
}
brasileirao["Pontos maximos"] = 80 // isso adiciona no objeto = propriedade
brasileirao.serieC = false // isso adiciona no objeto = propriedade
console.log(brasileirao)